pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,mjs,cjs,ts,json,yml,yaml,md}'
      run: pnpm format:check

pre-push:
  commands:
    review:
      run: bash scripts/review-diff.sh
